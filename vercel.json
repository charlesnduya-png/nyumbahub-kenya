{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/ensure-db-env.js && prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm ci"
}
